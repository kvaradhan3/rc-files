prtiming() {
    e=$3
    e=${e:-$SECONDS}
    print -f ": %25s\t%8.6f -- %8.6f\t%8.6f\n" $1 $2 $e $(( $e - $2))
}
#
fpath=(
        $ZDOTDIR
        $ZDOTDIR_LOCAL
        $ZDOTDIR_LOCAL/functions 
        $HOME/src/zsh-completions/src
        /usr/local/share/zsh-completions
        $fpath
    )


autoload set_cdpath && set_cdpath
autoload addpath && addpath
autoload vlc
